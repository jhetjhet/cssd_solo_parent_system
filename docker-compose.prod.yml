services:
  backend:
    command: sh -c "gunicorn solo_parnt_system.wsgi:application --bind 0.0.0.0:8000 --workers 3"

  frontend:
    depends_on:
      - backend
      - worker
    ports:
      - "3005:80"